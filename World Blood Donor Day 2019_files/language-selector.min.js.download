"use strict";

function openLinkWithTranslation(culture) {
  var url = $('[data-custom-language-selector="' + culture + '"]').val();

  if (url == null) {
    url = "/" + culture;
  }

  window.location = url;
}

(function ($) {
  $(function () {
    var detailItemCultures = [];
    $('input[data-custom-language-selector]').each(function () {
      var culture = $(this).data('custom-language-selector');
      $('a[data-available-culture]').each(function (index, element) {
        var currentCulture = $(this).data('available-culture');

        if (currentCulture == culture) {
          $(element).css('display', 'inline-block');
        }
      });
    });

    if ($("a[data-available-culture][class=' details']").length > 0) {
      $("a[data-available-culture][class=' header']").each(function (index, element) {
        var currentHeaderCulture = $(this).data('available-culture');
        var cultureExists = false;
        $("a[data-available-culture][class=' details']").each(function (i, e) {
          var currentDetailsCulture = $(this).data('available-culture');

          if (currentDetailsCulture == currentHeaderCulture) {
            if (e.style.display != "none") cultureExists = true;
          }
        });
        if (!cultureExists) $(element).addClass("disabled");
      });
    }
  });
})(jQuery);
//# sourceMappingURL=language-selector.min.js.map
