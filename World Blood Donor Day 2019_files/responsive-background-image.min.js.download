"use strict";

(function () {
  var responsiveBackgroundImage = function responsiveBackgroundImage(element) {
    var newElement = element;
    var img = newElement.querySelector('img');
    var src = '';
    img.addEventListener('load', function () {
      update(img, src, element);
    });

    if (img.complete) {
      update(img, src, element);
    }
  };

  var update = function update(img, src, element) {
    var newSrc = typeof img.currentSrc !== 'undefined' ? img.currentSrc : img.src;

    if (src !== newSrc) {
      src = newSrc;
      element.style.backgroundImage = 'url("' + src + '")';
    }
  };

  var elements = document.querySelectorAll('.responsive-background-image');

  for (var i = 0; i < elements.length; i++) {
    responsiveBackgroundImage(elements[i]);
  }
})();
//# sourceMappingURL=responsive-background-image.min.js.map
